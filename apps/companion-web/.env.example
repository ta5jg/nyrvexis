# Local playtest — copy to `.env` or `.env.local` (Vite loads both).
# Gateway must run on this host:port (pnpm run dev:full from repo root).

VITE_GATEWAY_URL=http://127.0.0.1:8787

# Optional — Google Sign-In (same Web Client ID as gateway KR_GOOGLE_CLIENT_ID)
# VITE_GOOGLE_CLIENT_ID=
